<h1>Delete User</h1>

<p>
  Are you sure you want to delete the user:
  <strong><%= @user.username %></strong>?
</p>

<div style="margin-top: 1rem;">
  <%= button_to "Yes, delete this user",
                @user,
                method: :delete,
                form: { data: { turbo_confirm: "This cannot be undone. Continue?" } } %>
</div>

<p style="margin-top: 1rem;">
  <%= link_to "Cancel and go back to Users", users_path %>
</p>
